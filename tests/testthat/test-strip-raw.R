# load(testthat::test_path("data/datasets.rda"))
# library(mgcv)
#
# test_that("strip-raw works", {
#   fit_1 <- fit1 <- gam(y ~ s(x0, k = 8, bs = "cr") + s(x1, bs = "cr") + s(x2, bs = "cr"),
#                        data = datasets[[1]])
#
#   fit_no_raw1 <- strip_rawdata(fit1)
#
# })
